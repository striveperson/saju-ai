<!--
해당 없는 절은 통째로 지운다. 빈 항목이나 "해당 없음" 을 남기지 않는다.
자명한 PR 은 "무엇을 왜" 만으로 끝난다. 절 수로 성의를 재지 않는다.
-->

## 무엇을 왜

<!-- 이 변경이 없으면 무엇이 문제인지. 무엇을 했는지는 diff 가 말한다 -->

## 커밋

<!--
커밋이 여럿일 때만 쓴다. 하나면 지운다.
파일 목록을 옮겨 적지 않는다. 커밋마다 한 줄이면 된다.
-->

## 도메인 규칙

<!--
사주 계산 규칙을 건드렸을 때만 쓴다. 아니면 이 절을 지운다.
근거가 된 docs/05-saju-domain-rules.md 의 장 번호나 ADR 번호를 적는다.
유파 옵션(진태양시, 야자시, 지원 세력, 대운수)에 영향이 있으면 함께 적는다.
-->

## Breaking Change

<!--
호환성이 깨질 때만 쓴다. 아니면 이 절을 지운다.
무엇이 깨지고 호출부를 어떻게 바꿔야 하는지. 커밋 본문의 BREAKING CHANGE 와 같은 내용이어야 한다.
-->

## 스크린샷

<!-- UI 가 바뀌었을 때만 쓴다. 아니면 이 절을 지운다 -->

## 관련 이슈

<!-- Closes #42, Refs #17. 없으면 이 절을 지운다 -->
